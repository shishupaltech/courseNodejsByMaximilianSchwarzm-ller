// templating engines 
    // htmllist template 

    // node/express conent          templating engine 
                // replaces placeholders/snippets 
                //     with html content
                    // html file 
    // avaible templing engines 
    // ejs                 pug(jode)                      handlers 

    //<p><%=name%></p>   p#{name}  <p> {{name}}</p>

    // ejs use normal html and plain javascript in your templates 

    // pug(jode) use minimal html and custom template language 

    // handlebars use normal html and custom template language.